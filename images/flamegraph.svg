<svg xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" width="1200" height="182" onload="init(evt)" version="1.1" viewBox="0 0 1200 182"><defs><linearGradient id="background" x1="0" x2="0" y1="0" y2="1"><stop offset="5%" stop-color="#eee"/><stop offset="95%" stop-color="#eeeeb0"/></linearGradient></defs><style type="text/css">text { font-family:&quot;Verdana&quot;; font-size:12px; fill:rgb(0,0,0); } #title { text-anchor:middle; font-size:17px; } #search { opacity:0.1; cursor:pointer; } #search:hover, #search.show { opacity:1; } #subtitle { text-anchor:middle; font-color:rgb(160,160,160); } #unzoom { cursor:pointer; } #frames &gt; *:hover { stroke:black; stroke-width:0.5; cursor:pointer; } .hide { display:none; } .parent { opacity:0.5; }</style><script type="text/ecmascript"><![CDATA[var nametype = 'Function:';
var fontsize = 12;
var fontwidth = 0.59;
var xpad = 10;
var inverted = true;
var searchcolor = 'rgb(230,0,230)';]]><![CDATA["use strict";
var details, searchbtn, unzoombtn, matchedtxt, svg, searching;
function init(evt) {
    details = document.getElementById("details").firstChild;
    searchbtn = document.getElementById("search");
    unzoombtn = document.getElementById("unzoom");
    matchedtxt = document.getElementById("matched");
    svg = document.getElementsByTagName("svg")[0];
    searching = 0;
}

window.addEventListener("click", function(e) {
	var target = find_group(e.target);
	if (target) {
        if (target.nodeName == "a") {
            if (e.ctrlKey === false) return;
            e.preventDefault();
        }
        if (target.classList.contains("parent")) unzoom();
        zoom(target);
    }
    else if (e.target.id == "unzoom") unzoom();
    else if (e.target.id == "search") search_prompt();
}, false)

// mouse-over for info
// show
window.addEventListener("mouseover", function(e) {
	var target = find_group(e.target);
	if (target) details.nodeValue = nametype + " " + g_to_text(target);
}, false)
// clear
window.addEventListener("mouseout", function(e) {
	var target = find_group(e.target);
	if (target) details.nodeValue = ' ';
}, false)
// ctrl-F for search
window.addEventListener("keydown",function (e) {
    if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
        e.preventDefault();
        search_prompt();
    }
}, false)
// functions
function find_child(node, selector) {
	var children = node.querySelectorAll(selector);
	if (children.length) return children[0];
	return;
}
function find_group(node) {
	var parent = node.parentElement;
	if (!parent) return;
	if (parent.id == "frames") return node;
	return find_group(parent);
}
function orig_save(e, attr, val) {
    if (e.attributes["_orig_" + attr] != undefined) return;
    if (e.attributes[attr] == undefined) return;
    if (val == undefined) val = e.attributes[attr].value;
    e.setAttribute("_orig_" + attr, val);
}
function orig_load(e, attr) {
    if (e.attributes["_orig_"+attr] == undefined) return;
    e.attributes[attr].value = e.attributes["_orig_" + attr].value;
    e.removeAttribute("_orig_" + attr);
}
function g_to_text(e) {
    var text = find_child(e, "title").firstChild.nodeValue;
    return (text)
}
function g_to_func(e) {
    var func = g_to_text(e);
    // if there's any manipulation we want to do to the function
    // name before it's searched, do it here before returning.
    return (func);
}
function update_text(e) {
    var r = find_child(e, "rect");
    var t = find_child(e, "text");
    var w = parseFloat(r.attributes.width.value) -3;
    var txt = find_child(e, "title").textContent.replace(/\\([^(]*\\)\$/,"");
    t.attributes.x.value = parseFloat(r.attributes.x.value) + 3;
    // Smaller than this size won't fit anything
    if (w < 2 * fontsize * fontwidth) {
        t.textContent = "";
        return;
    }
    t.textContent = txt;
    // Fit in full text width
    if (/^ *\$/.test(txt) || t.getSubStringLength(0, txt.length) < w)
        return;
    for (var x = txt.length - 2; x > 0; x--) {
        if (t.getSubStringLength(0, x + 2) <= w) {
            t.textContent = txt.substring(0, x) + "..";
            return;
        }
    }
    t.textContent = "";
}
// zoom
function zoom_reset(e) {
    if (e.attributes != undefined) {
        orig_load(e, "x");
        orig_load(e, "width");
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_reset(c[i]);
    }
}
function zoom_child(e, x, ratio) {
    if (e.attributes != undefined) {
        if (e.attributes.x != undefined) {
            orig_save(e, "x");
            e.attributes.x.value = (parseFloat(e.attributes.x.value) - x - xpad) * ratio + xpad;
            if(e.tagName == "text")
                e.attributes.x.value = find_child(e.parentNode, "rect[x]").attributes.x.value + 3;
        }
        if (e.attributes.width != undefined) {
            orig_save(e, "width");
            e.attributes.width.value = parseFloat(e.attributes.width.value) * ratio;
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_child(c[i], x - xpad, ratio);
    }
}
function zoom_parent(e) {
    if (e.attributes) {
        if (e.attributes.x != undefined) {
            orig_save(e, "x");
            e.attributes.x.value = xpad;
        }
        if (e.attributes.width != undefined) {
            orig_save(e, "width");
            e.attributes.width.value = parseInt(svg.width.baseVal.value) - (xpad*2);
        }
    }
    if (e.childNodes == undefined) return;
    for(var i = 0, c = e.childNodes; i < c.length; i++) {
        zoom_parent(c[i]);
    }
}
function zoom(node) {
    var attr = find_child(node, "rect").attributes;
    var width = parseFloat(attr.width.value);
    var xmin = parseFloat(attr.x.value);
    var xmax = parseFloat(xmin + width);
    var ymin = parseFloat(attr.y.value);
    var ratio = (svg.width.baseVal.value - 2 * xpad) / width;
    // XXX: Workaround for JavaScript float issues (fix me)
    var fudge = 0.0001;
    unzoombtn.classList.remove("hide");
    var el = document.getElementById("frames").children;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var a = find_child(e, "rect").attributes;
        var ex = parseFloat(a.x.value);
        var ew = parseFloat(a.width.value);
        // Is it an ancestor
        if (!inverted) {
            var upstack = parseFloat(a.y.value) > ymin;
        } else {
            var upstack = parseFloat(a.y.value) < ymin;
        }
        if (upstack) {
            // Direct ancestor
            if (ex <= xmin && (ex+ew+fudge) >= xmax) {
                e.classList.add("parent");
                zoom_parent(e);
                update_text(e);
            }
            // not in current path
            else
                e.classList.add("hide");
        }
        // Children maybe
        else {
            // no common path
            if (ex < xmin || ex + fudge >= xmax) {
                e.classList.add("hide");
            }
            else {
                zoom_child(e, xmin, ratio);
                update_text(e);
            }
        }
    }
}
function unzoom() {
    unzoombtn.classList.add("hide");
    var el = document.getElementById("frames").children;
    for(var i = 0; i < el.length; i++) {
        el[i].classList.remove("parent");
        el[i].classList.remove("hide");
        zoom_reset(el[i]);
        update_text(el[i]);
    }
}
// search
function reset_search() {
    var el = document.querySelectorAll("#frames rect");
    for (var i = 0; i < el.length; i++) {
        orig_load(el[i], "fill")
    }
}
function search_prompt() {
    if (!searching) {
        var term = prompt("Enter a search term (regexp " +
            "allowed, eg: ^ext4_)", "");
        if (term != null) {
            search(term)
        }
    } else {
        reset_search();
        searching = 0;
        searchbtn.classList.remove("show");
        searchbtn.firstChild.nodeValue = "Search"
        matchedtxt.classList.add("hide");
        matchedtxt.firstChild.nodeValue = ""
    }
}
function search(term) {
    var re = new RegExp(term);
    var el = document.getElementById("frames").children;
    var matches = new Object();
    var maxwidth = 0;
    for (var i = 0; i < el.length; i++) {
        var e = el[i];
        var func = g_to_func(e);
        var rect = find_child(e, "rect");
        if (func == null || rect == null)
            continue;
        // Save max width. Only works as we have a root frame
        var w = parseFloat(rect.attributes.width.value);
        if (w > maxwidth)
            maxwidth = w;
        if (func.match(re)) {
            // highlight
            var x = parseFloat(rect.attributes.x.value);
            orig_save(rect, "fill");
            rect.attributes.fill.value = searchcolor;
            // remember matches
            if (matches[x] == undefined) {
                matches[x] = w;
            } else {
                if (w > matches[x]) {
                    // overwrite with parent
                    matches[x] = w;
                }
            }
            searching = 1;
        }
    }
    if (!searching)
        return;
    searchbtn.classList.add("show");
    searchbtn.firstChild.nodeValue = "Reset Search";
    // calculate percent matched, excluding vertical overlap
    var count = 0;
    var lastx = -1;
    var lastw = 0;
    var keys = Array();
    for (k in matches) {
        if (matches.hasOwnProperty(k))
            keys.push(k);
    }
    // sort the matched frames by their x location
    // ascending, then width descending
    keys.sort(function(a, b){
        return a - b;
    });
    // Step through frames saving only the biggest bottom-up frames
    // thanks to the sort order. This relies on the tree property
    // where children are always smaller than their parents.
    var fudge = 0.0001;	// JavaScript floating point
    for (var k in keys) {
        var x = parseFloat(keys[k]);
        var w = matches[keys[k]];
        if (x >= lastx + lastw - fudge) {
            count += w;
            lastx = x;
            lastw = w;
        }
    }
    // display matched percent
    matchedtxt.classList.remove("hide");
    var pct = 100 * count / maxwidth;
    if (pct != 100) pct = pct.toFixed(1);
    matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
}
]]></script><rect width="1200" height="182" x="0" y="0" fill="url(#background)"/><text id="title" x="600" y="24">py-spy</text><text id="unzoom" x="10" y="24" class="hide">Reset Zoom</text><text id="search" x="1090" y="24">Search</text><g id="frames"><g><title>fit (implicit/als.py:130) (48 samples, 1.66%)</title><rect width="19" height="15" x="10" y="84" fill="#D1A52A"/></g><g><title>tocsr (scipy/sparse/csc.py:151) (48 samples, 1.66%)</title><rect width="19" height="15" x="10" y="100" fill="#E0320E"/></g><g><title>fit (implicit/als.py:138) (8 samples, 0.28%)</title><rect width="3" height="15" x="29" y="84" fill="#E23A18"/></g><g><title>fit (implicit/als.py:140) (5 samples, 0.17%)</title><rect width="2" height="15" x="32" y="84" fill="#F61E2B"/></g><g><title>fit (implicit/als.py:159) (625 samples, 21.60%)</title><rect width="255" height="15" x="34" y="84" fill="#E91810"/><text x="37" y="94.5">fit (implicit/als.py:159)</text></g><g><title>calculate_similar_artists (lastfm.py:79) (1,490 samples, 51.50%)</title><rect width="607" height="15" x="10" y="68" fill="#D1D82C"/><text x="13" y="78.5">calculate_similar_artists (lastfm.py:79)</text></g><g><title>fit (implicit/als.py:163) (804 samples, 27.79%)</title><rect width="328" height="15" x="289" y="84" fill="#FCE32E"/><text x="292" y="94.5">fit (implicit/als.py:163)</text></g><g><title>calculate_similar_artists (lastfm.py:87) (20 samples, 0.69%)</title><rect width="8" height="15" x="617" y="68" fill="#DBA415"/></g><g><title>&lt;lambda&gt; (lastfm.py:87) (7 samples, 0.24%)</title><rect width="2" height="15" x="623" y="84" fill="#E65F15"/></g><g><title>similar_items (implicit/recommender_base.py:201) (5 samples, 0.17%)</title><rect width="2" height="15" x="625" y="84" fill="#ED3D0C"/></g><g><title>item_norms (implicit/recommender_base.py:223) (5 samples, 0.17%)</title><rect width="2" height="15" x="625" y="100" fill="#CF2729"/></g><g><title>norm (numpy/linalg/linalg.py:2389) (5 samples, 0.17%)</title><rect width="2" height="15" x="625" y="116" fill="#F7B81E"/></g><g><title>_get_similarity_score (implicit/recommender_base.py:208) (545 samples, 18.84%)</title><rect width="222" height="15" x="628" y="100" fill="#E84A2D"/><text x="631" y="110.5">_get_similarity_score (implic..</text></g><g><title>_get_similarity_score (implicit/recommender_base.py:209) (814 samples, 28.14%)</title><rect width="332" height="15" x="850" y="100" fill="#F89E10"/><text x="853" y="110.5">_get_similarity_score (implicit/recommender_..</text></g><g><title>argpartition (numpy/core/fromnumeric.py:757) (813 samples, 28.10%)</title><rect width="331" height="15" x="851" y="116" fill="#F8DF18"/><text x="854" y="126.5">argpartition (numpy/core/fromnumeric.py:757)</text></g><g><title>_wrapfunc (numpy/core/fromnumeric.py:51) (813 samples, 28.10%)</title><rect width="331" height="15" x="851" y="132" fill="#D6B32D"/><text x="854" y="142.5">_wrapfunc (numpy/core/fromnumeric.py:51)</text></g><g><title>calculate_similar_artists (lastfm.py:95) (1,371 samples, 47.39%)</title><rect width="560" height="15" x="625" y="68" fill="#E47E2A"/><text x="628" y="78.5">calculate_similar_artists (lastfm.py:95)</text></g><g><title>similar_items (implicit/recommender_base.py:203) (1,366 samples, 47.22%)</title><rect width="558" height="15" x="627" y="84" fill="#E17F18"/><text x="630" y="94.5">similar_items (implicit/recommender_base.py:203)</text></g><g><title>_get_similarity_score (implicit/recommender_base.py:210) (6 samples, 0.21%)</title><rect width="3" height="15" x="1182" y="100" fill="#E89405"/></g><g><title>calculate_similar_artists (lastfm.py:96) (11 samples, 0.38%)</title><rect width="4" height="15" x="1185" y="68" fill="#D72220"/></g><g><title>write (codecs.py:721) (4 samples, 0.14%)</title><rect width="2" height="15" x="1187" y="84" fill="#E60F11"/></g><g><title>all (2,893 samples, 100%)</title><rect width="1180" height="15" x="10" y="36" fill="#DA3115"/></g><g><title>&lt;module&gt; (lastfm.py:161) (2,893 samples, 100.00%)</title><rect width="1180" height="15" x="10" y="52" fill="#D52826"/><text x="13" y="62.5">&lt;module&gt; (lastfm.py:161)</text></g></g></svg>